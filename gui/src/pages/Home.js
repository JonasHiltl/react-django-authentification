import React from 'react';

import { Typography } from 'antd';
import './home.less'

const { Title } = Typography;

const Home = () => {
    return (
        <div>
            <Title>Homepage</Title>
        </div>
    );
};

export default Home;